<table>
   <caption>Nombre de posts: <%= @posts.count %> </caption>
   <thead>
       <tr>
           <th>Titre</th>
           <th>Date</th>
           <th>Âge</th>
           <th>Chapeau</th>
           <th>Vignette</th>
           <th>Afficher</th>
       </tr>
   </thead>
   <tbody>
       <% @posts.each do |post| %>
       <tr>
           <td><%= post.titre %></td>
           <td><%= post.date %></td>
           <td><%= post.chapeau %></td>
           <td><%= post.vignette %></td>
           <td><%= post.auteur %></td>
           <td><%= link_to 'Afficher', post_path(user) %></td>
       </tr>
       <% end %>
   </tbody>
</table>
